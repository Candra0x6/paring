---
phase: 01-nurse-registration
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [web/app/register/page.tsx]
autonomous: true
requirements: [NURSE-04, NURSE-05, NURSE-06]
must_haves:
  truths:
    - "User can navigate between steps 1-5"
    - "Step 5 displays a correct summary of entered data"
    - "Final confirmation redirects to /nurse/dashboard"
  artifacts:
    - path: "web/app/register/page.tsx"
      provides: "Final steps and redirection logic"
---

<objective>
Implement the remaining nurse detailing steps and final redirection.
Purpose: Complete the nurse profile setup with document placeholders and availability preferences.
Output: Fully functional 5-step registration process.
</objective>

<execution_context>
@/home/cn/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@web/app/register/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Step 3 (Document Upload) and Step 4 (Availability/Preferences)</name>
  <files>web/app/register/page.tsx</files>
  <action>
    - Implement Step 3: Document placeholders for:
      - STR (Surat Tanda Registrasi).
      - KTP (Identity Card).
      - Ijazah / Sertifikat Pelatihan.
      - Add a generic upload button styled with Lucide icons (UploadCloud).
    - Implement Step 4: Preferences for:
      - Service Types (Visit, Live-in, Live-out).
      - Working Hours / Availability summary (Textarea or simple select).
      - Preferred locations or region.
    - Add "Lanjutkan" and "Kembali" buttons for each step.
  </action>
  <verify>
    - Step 3 shows document upload placeholders.
    - Step 4 allows selection of service types.
  </verify>
  <done>Remaining detail collection steps (3-4) are functional and styled.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Step 5 (Summary/Review) and Finalize Redirect</name>
  <files>web/app/register/page.tsx</files>
  <action>
    - Implement Step 5: A summary view displaying all data from `nurseData`:
      - Account Info (Name, Email, WhatsApp).
      - Detailing (Experience, Specialization, Bio).
      - Availability and Service Types.
    - Provide a "Konfirmasi & Selesai" button.
    - Handle the final click by:
      - Storing all data in localStorage (simulating an API call).
      - Setting `userRole` to 'NURSE'.
      - Redirecting to `/nurse/dashboard`.
  </action>
  <verify>
    - Step 5 correctly displays the entered information.
    - Clicking the final button redirects the user to '/nurse/dashboard'.
  </verify>
  <done>Full nurse registration process is completed and redirects to the correct dashboard.</done>
</task>

</tasks>

<success_criteria>
1. Steps 3, 4, and 5 are fully implemented.
2. Data persistence across steps is maintained.
3. Summary step correctly reflects all user inputs.
4. Final redirection to /nurse/dashboard is successful.
</success_criteria>

<output>
After completion, create `.planning/phases/01-nurse-registration/01-02-SUMMARY.md`
</output>
