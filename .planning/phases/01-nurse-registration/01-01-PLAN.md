---
phase: 01-nurse-registration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [web/app/register/page.tsx]
autonomous: true
requirements: [NURSE-01, NURSE-02, NURSE-03]
must_haves:
  truths:
    - "User can see the step indicator after choosing NURSE and clicking 'Daftar Sekarang'"
    - "Step 1 shows a welcome message"
    - "Step 2 allows input of experience and specialization"
  artifacts:
    - path: "web/app/register/page.tsx"
      provides: "Nurse registration stepper component"
---

<objective>
Implement the stepper infrastructure and the first two steps of nurse registration.
Purpose: Guide nurses through a detailed registration process to collect necessary profile data.
Output: Refactored registration page with a functional stepper state and first two steps.
</objective>

<execution_context>
@/home/cn/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@web/app/register/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add stepper state and basic UI structure</name>
  <files>web/app/register/page.tsx</files>
  <action>
    - Add `step` and `nurseData` states.
    - If `role === 'NURSE'`, modify `handleRegister` to set `step` to 1 instead of redirecting.
    - Create a wrapper that renders either Step 0 (initial form) or the Nurse Registration Stepper based on `step`.
    - Implement a progress indicator (1/5, 2/5, etc.) for the nurse steps.
  </action>
  <verify>
    - Clicking 'Daftar Sekarang' as a NURSE shows Step 1.
    - Clicking 'Daftar Sekarang' as a PATIENT still redirects to '/dashboard'.
  </verify>
  <done>Stepper logic is integrated into the registration page.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Step 1 (Intro) and Step 2 (Personal Detailing)</name>
  <files>web/app/register/page.tsx</files>
  <action>
    - Implement Step 1: Welcome message with an icon and a brief explanation ("Mari lengkapi profil Anda agar keluarga dapat mengenal Anda lebih baik").
    - Implement Step 2: Form fields for:
      - Years of Experience (Input number).
      - Specialization (e.g., Lansia, Stroke, Bedridden - use Checkbox or Multi-select).
      - Short Bio (Textarea).
    - Add "Lanjutkan" button to advance to the next step.
    - Add "Kembali" button to return to the previous step.
    - Style according to the existing design (Vanilla CSS/Lucide).
  </action>
  <verify>
    - Step 1 shows welcome text and a 'Lanjutkan' button.
    - Step 2 shows input fields and correctly updates the `nurseData` state.
  </verify>
  <done>First two steps of nurse detailing are functional and styled.</done>
</task>

</tasks>

<success_criteria>
1. Stepper state management is working for Nurse role.
2. Step 1 (Welcome) and Step 2 (Personal Detailing) are implemented with appropriate form fields.
3. Navigation between steps (0 -> 1 -> 2) works correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-nurse-registration/01-01-SUMMARY.md`
</output>
